var title = document.getElementById('title');


if(document.title === "Bestand"){
var title = document.getElementById('title');
        title.textContent = '' + document.title;
} else if(document.title === "Dashboard"){
    title.textContent = '' + document.title;
}